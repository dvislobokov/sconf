# Имитация секретов, которые монтирует sidecar (например Vault Agent).
# Подключается отдельным слоем с Optional() + при необходимости Wait().

[database]
password = "s3cr3t-from-vault"

[redis]
password = "redis-vault-pass"
